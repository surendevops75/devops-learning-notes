# Amazon Simple Storage Service (Amazon S3)

---

# Introduction

Amazon Simple Storage Service (Amazon S3) is a fully managed, highly durable, scalable, secure, and cost-effective object storage service provided by AWS.

Unlike Amazon EBS and Amazon EFS, which are block and file storage services respectively, Amazon S3 stores data as **objects** inside **buckets**. It is designed to store virtually unlimited amounts of structured and unstructured data while providing high durability, availability, and security.

Amazon S3 is one of the oldest and most widely used AWS services. It is the storage backbone for many AWS services and is commonly used for application assets, backups, logs, static websites, media files, data lakes, machine learning datasets, and disaster recovery.

---

# What is Amazon S3?

Amazon S3 is an **Object Storage Service**.

Everything stored in Amazon S3 is called an **Object**.

Objects are stored inside **Buckets**.

Example:

```
Bucket

├── image1.png

├── invoice.pdf

├── backup.zip

├── logs/

│      ├── app.log

│      └── nginx.log

└── videos/

       └── demo.mp4
```

Unlike traditional file systems, S3 has no real directory hierarchy. Folder structures are logical prefixes created as part of the object key.

---

# Why Amazon S3?

Imagine a web application running on multiple EC2 instances.

```
EC2-1

↓

Uploads image

↓

Local Disk
```

When another server receives a request:

```
EC2-2

↓

Image Missing
```

Local storage causes inconsistency.

Using Amazon S3:

```
Application

↓

Amazon S3 Bucket

↓

Any EC2 Instance

↓

Read Same Object
```

Every application server accesses the same object regardless of which server processed the upload.

---

# Real World Problem

An online shopping platform stores:

- Product Images
- Customer Documents
- Bills
- Reports
- Videos
- Application Backups

Requirements:

- Unlimited Storage
- High Availability
- Encryption
- Low Cost
- Global Access
- Disaster Recovery

Amazon S3 satisfies all of these requirements without managing storage infrastructure.

---

# Complete Architecture

```text
                       Users
                          │
                   Amazon Route53
                          │
                    CloudFront CDN
                          │
                    AWS WAF (Optional)
                          │
                   Application Load Balancer
                          │
                Auto Scaling Group (EC2/EKS)
                          │
                Upload / Download Objects
                          │
                  Amazon S3 Bucket
        ┌─────────────────┼─────────────────┐
        │                 │                 │
 Versioning         Lifecycle Policy   Cross Region Replication
        │                 │                 │
     AWS KMS         Glacier Archive    Disaster Recovery
        │                 │                 │
   CloudTrail         CloudWatch       AWS Backup
```

---

# Object Storage vs Block Storage vs File Storage

| Feature | Amazon S3 | Amazon EBS | Amazon EFS |
|----------|-----------|------------|------------|
| Storage Type | Object | Block | File |
| Mountable | No | Yes | Yes |
| Shared Storage | Via API | No | Yes |
| Capacity | Virtually Unlimited | Fixed | Elastic |
| Protocol | HTTP/HTTPS | Block Device | NFS |
| Use Case | Images, Logs, Backups | OS, Databases | Shared Files |

---

# Internal Working

When a client uploads an object:

```text
Application

↓

HTTPS Request

↓

Amazon S3 API

↓

Authentication

↓

Bucket Validation

↓

Storage Layer

↓

Object Metadata

↓

Object Storage

↓

Success Response
```

Each uploaded object consists of:

- Object Key
- Object Data
- Metadata
- Version ID (optional)
- Storage Class
- Encryption Information

---

# Core Components

Amazon S3 consists of:

- Buckets
- Objects
- Object Keys
- Metadata
- Version IDs
- Storage Classes
- Bucket Policies
- Lifecycle Rules
- Replication Rules
- Access Points

---

# Buckets

A Bucket is a container that stores objects.

Characteristics:

- Globally unique name
- Region-specific
- Unlimited objects
- IAM controlled
- Supports lifecycle policies

Examples:

```
company-prod-storage

company-backups

terraform-state

jenkins-artifacts

application-logs
```

---

# Objects

An Object is the actual file stored in S3.

Examples:

```
logo.png

invoice.pdf

backup.tar.gz

logs.txt

terraform.tfstate
```

Maximum object size:

**5 TB**

---

# Object Key

The Object Key uniquely identifies an object.

Example:

```
images/profile/user1.png

logs/nginx/access.log

backup/2026/db.sql.gz
```

Although it appears hierarchical, S3 stores it as a flat namespace using prefixes.

---

# Metadata

Every object contains metadata.

Examples:

- Content-Type
- Content-Length
- Last Modified
- Encryption
- Cache-Control
- User-defined Metadata

Metadata helps applications identify and process objects efficiently.

---

# Durability

Amazon S3 is designed for **11 nines (99.999999999%) durability**.

AWS automatically stores multiple copies of each object across multiple devices within the selected Region.

If a storage device fails, S3 automatically recreates redundant copies without user intervention.

---

# Availability

Amazon S3 provides high availability through redundant infrastructure within an AWS Region.

Applications can continue accessing data even if individual storage devices fail.

---

# Scalability

Amazon S3 automatically scales without manual intervention.

You never need to:

- Add disks
- Resize storage
- Create RAID arrays
- Increase capacity manually

Storage grows automatically as objects are uploaded.

---

# Data Consistency

Amazon S3 provides **strong read-after-write consistency** for:

- PUT
- DELETE
- LIST

Once an upload completes successfully, any subsequent read immediately returns the latest version of the object.

---

# Features

Amazon S3 provides:

- Unlimited Storage
- High Durability
- High Availability
- Versioning
- Lifecycle Policies
- Object Lock
- Replication
- Static Website Hosting
- Event Notifications
- Encryption
- Access Logging
- Bucket Policies
- IAM Integration
- CloudTrail Integration
- CloudWatch Metrics
- Transfer Acceleration

---

